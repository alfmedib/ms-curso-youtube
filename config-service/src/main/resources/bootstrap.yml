server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git: https://github.com/alfmedib/ms-curso-youtube.git
        searchPaths: config-data
        username: ${GIT_USER}
        password: ${GIT_PASSWORD}

    security:
      user:
        name: root
        password: s3cr3t
