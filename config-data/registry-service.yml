server:
  port: 8099

eureka:
  instance:
    hostname:
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eurecka.instance.hostname}:${server.port}/eureka/